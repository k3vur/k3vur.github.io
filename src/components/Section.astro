---
import { cn } from "../lib/cn";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<section
  class={cn("text-ink-soft gap-s2 text-body m-0 flex flex-col", className)}
>
  <slot />
</section>
